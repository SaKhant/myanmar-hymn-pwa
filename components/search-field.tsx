import { Search } from "lucide-react";

export function SearchField({ value, onChange, onSubmit, placeholder="Search by number, title, or lyric…", autoFocus=false, cleanFocus=false }:{ value:string; onChange:(value:string)=>void; onSubmit?:()=>void; placeholder?:string; autoFocus?:boolean; cleanFocus?:boolean }) {
  return <form role="search" className="relative" onSubmit={(event)=>{event.preventDefault();onSubmit?.()}}><label className="block"><span className="sr-only">Search hymns</span><input type="text" lang="my" dir="ltr" autoComplete="off" autoCapitalize="none" autoCorrect="off" spellCheck={false} enterKeyHint="search" autoFocus={autoFocus} value={value} onChange={(event)=>onChange(event.currentTarget.value)} onKeyDown={(event)=>{if(event.key==="Enter"&&!event.nativeEvent.isComposing){event.preventDefault();event.currentTarget.form?.requestSubmit()}}} placeholder={placeholder} className={`myanmar-search-input h-14 w-full rounded-2xl border border-[var(--line)] bg-[var(--paper)] pl-4 pr-14 text-base ${cleanFocus?"search-input-clean":"focus-ring"}`} /></label><button type="submit" aria-label="Search" className="focus-ring absolute right-1 top-1 flex h-12 w-12 items-center justify-center rounded-xl text-[var(--muted)] hover:text-[var(--ink)]"><Search aria-hidden="true" size={21}/></button></form>;
}
